# Server Configuration
PORT=3001
HOST=0.0.0.0

# CORS Configuration
# For development/local network, use '*' to allow all origins
# For production, specify allowed origins separated by commas
# Example: ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.100:3000
ALLOWED_ORIGINS=*

# Game Configuration
# Available options for letter time (comma-separated, in seconds)
LETTER_TIME_OPTIONS=3,5,7,10,15
# Default letter time
DEFAULT_LETTER_TIME=5

# Available options for word time (comma-separated, in seconds)
WORD_TIME_OPTIONS=15,20,30,45,60,90,120
# Default word time
DEFAULT_WORD_TIME=30

# Available options for rounds to win (comma-separated)
ROUNDS_OPTIONS=3,5,7,10
# Default rounds to win
DEFAULT_ROUNDS=5
